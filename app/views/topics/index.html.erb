<div class="container"></div>
	<div class="header">
		<h1>寇汀災情支援網</h1>
		<subtitle>Coding Recue Network</subtitle>
	</div>

	<%= link_to 'POST', new_topic_path, class: 'btn btn-success' %>

	<table>
		<tr>
			<th>Category</th>
			<th>Title</th>
			<th>Created at</th>
			<th></th>
		</tr>
			<% @topics.each do |t| %>
		<tr>
				<td>【<%= t.category.name %>】</td>
				<td><%= link_to t.title, topic_path(t) %></td>
				<td><%= t.created_at.to_date %></td>
				<td>
					<%= link_to 'EDIT', edit_topic_path(t), class: "btn btn-primary btn-sm" %>
				</td>
		</tr>
			<% end %>
	</table>		